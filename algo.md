# 基于多方安全计算的组会时间选定算法

## 使用场景
学生 **Alice** 和 **Bob** 需要与导师安排一次组会。为了避免泄露各自的时间安排，他们希望通过一种安全计算方法，找出一个双方都有空的时间。

---

## 算法流程

### 约定符号
1. 将所有可能的时间段（可精确到小时，或分为上午/下午等时间块）划分为 **$N$ 个时间槽**。
2. 定义：
   - Alice 的有空时间集合：
     $$
     A_1 = \{ta_1, ta_2, ...\}, \ (ta_1 < ta_2 < ... )
     $$
   - Bob 的有空时间集合：
     $$
     B_1 = \{tb_1, tb_2, ...\}, \ (tb_1 < tb_2 < ... )
     $$

---

### 步骤 1：初始化
1. Alice 选择一个大素数 $p$ 和 $N$ 个小于 $p$ 的素数集合：
   $$
   P = \{p_1, p_2, ..., p_N\}
   $$
2. Alice 将 $P$ 发送给 Bob。

---

### 步骤 2：Alice 生成加密集合
1. Alice 随机选择一个指数 $\alpha \in \{3,4,\dots,N-3\}$。
2. 将自己的时间集合 $A_1$ 映射为素数集合：
   $$
   A_2 = \{p_i | i \in A_1\}
   $$
3. 若 $|A_2| < N$，补充 $N - |A_2|$ 个 **哑元**素数（选取小于 $p$ 且不在 $P$ 中的素数），使得 $|A_2| = N$。
4. 计算：
   $$
   M_a = \{p^\alpha \mid p \in A_2\}
   $$
5. 将 $M_a$ 排序，并记录排序映射 $P_1$。
6. Alice 将排序后的 $M_a$ 发送给 Bob。

---

### 步骤 3：Bob 处理并生成加密集合
1. Bob 收到 $M_a$ 后，随机选择一个指数 $\beta \in \{3,4,\dots,N-3\}$。
2. 对 $M_a$ 中的每个元素进行加密：
   $$
   M_{ab} = \{m^\beta \bmod p \mid m \in M_a\}
   $$
3. 排序 $M_{ab}$，记录排序映射 $P_2$。
4. 将 $M_{ab}$ 发送给 ZY（第三方协调者）。
5. 同时，Bob 处理自己的时间集合：
   - 映射为素数集合：
     $$
     B_2 = \{p_i | i \in B_1\}
     $$
   - 若 $|B_2| < N$，补充 $N - |B_2|$ 个哑元素数。
   - 计算：
     $$
     M_b = \{p^\beta \mid p \in B_2\}
     $$
   - 排序后将 $M_b$ 发送给 Alice。

---

### 步骤 4：Alice 再次加密
1. Alice 收到 $M_b$ 后，计算：
   $$
   M_{ba} = \{m^\alpha \bmod p \mid m \in M_b\}
   $$
2. 排序 $M_{ba}$。
3. 将 $M_{ba}$ 发送给 ZY。

---

### 步骤 5：第三方协调者 (ZY) 求交集
1. ZY 计算：
   $$
   M_z = M_{ab} \cap M_{ba}
   $$
2. 任意选取一个元素 $z \in M_z$。
3. ZY 将 $z$ 在 $M_{ab}$ 中的位置索引 $i$ 发送给 Bob。

---

### 步骤 6：Bob 和 Alice 确定时间槽
1. Bob 根据 $i$ 在 $M_{ab}$ 中找到对应元素 $z$，再通过 $P_2$ 得到其原始索引 $j$。
2. Bob 将索引 $j$ 发送给 Alice。
3. Alice 根据 $j$ 在 $M_a$ 中找到对应元素 $m_j$，再通过 $P_1$ 确定原始素数 $p_k$。
4. 该素数对应的索引 $k$ 即为 **双方共有空闲的时间槽**。

---

### 特殊情况处理
- 若 $k > N$，说明 ZY 选取的元素来自双方补充的哑元,此情况发生概率极小且可恢复。
- **解决方法**：ZY 从交集中选择下一个元素重新执行确认过程。

---

## 优势
- 双方时间安排不泄露  
- 仅第三方协调者得知有交集，不知具体时间  
- 即使选取的时间槽冲突，仍能安全重试  
